{
  "dependencies": {
    "axios": "^1.3.3",
    "nyc": "^15.1.0",
    "octokit": "^2.0.14",
    "query-registry": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "typescript": "^4.9.5"
  },
  "name": "test",
  "version": "1.0.0",
  "main": "./api.js",
  "scripts": {
    "install-save": "npm i axios query-registry nyc octokit --save",
    "install-dev": "npm i typescript @types/node --save-dev",
    "install": "npm run -s install-save && npm run -s install-dev && pip3 install pandas javascript ndjson coverage python-dotenv",
    "build": "npx tsc",
    "clear": "rm -rf ./node_modules ./package-lock.json ./contribute.json ./language.json ./license.json ./metrics.json ./readme.json ./Scorecard.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
